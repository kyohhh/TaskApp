<header class="header">
  <nav class="nav">
    <ul class="header_menu">
      <li class="nav-link">
        <%# ゲストユーザーの場合リンクをなくす編集削除できないようにする %>
        <% if current_user.name == "ゲスト" %>
          <%= current_user.name %>さん</li>
        <% else %>
          <%= link_to current_user.name, edit_user_registration_path %>さん</li>
        <% end %>
      <li class="header_menu_title"><%= link_to "TaskApp", root_path, class: "nav-link" %></li>
      <li>
        <ul class="header_menu_inner">
          <%# リストを作成するリンクは今後開発していくので、"#" %>
          <li>
            <%= link_to new_list_path, class: "nav-link" do %>
              <span class="mr-1">リストを作成</span>
              <i class="fas fa-edit"></i>
            <% end %>
          </li>
          <li><%= link_to "サインアウト", destroy_user_session_path, class: "nav-link", method: :delete %></li>
        </ul>
      </li>
    </ul>
  </nav>
</header>
